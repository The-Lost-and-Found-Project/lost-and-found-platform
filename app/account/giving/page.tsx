import Link from"next/link";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";
export default async function GivingAccount(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect("/login?next=/account/giving");const email=user.email?.trim().toLowerCase()??"";const{data:x,error}=await s.from("giving_transactions").select("id,amount,currency,donated_at,transaction_type,status,receipt_url,source").or(`user_id.eq.${user.id}${email?`,contact_email.ilike.${email}`:""}`).order("donated_at",{ascending:false});const year=new Date().getFullYear(),ytd=(x??[]).filter((t:any)=>new Date(t.donated_at).getFullYear()===year&&t.status==="completed").reduce((n:any,t:any)=>n+Number(t.amount),0);return <main className="lfp-page pb-24"><div className="lfp-shell py-10"><Link href="/account" className="text-sm font-black text-blue-700">← Account</Link><p className="lfp-eyebrow mt-8">My Giving</p><h1 className="mt-3 text-4xl font-black">Your giving record with L&amp;F.</h1><p className="mt-3 max-w-3xl text-slate-600">This report reflects transactions synchronized from Zeffy. Zeffy remains the official payment and receipt source.</p><div className="mt-7 lfp-card p-6"><p className="text-xs font-black uppercase tracking-widest text-slate-500">{year} year to date</p><p className="mt-2 text-4xl font-black">{ytd.toLocaleString("en-US",{style:"currency",currency:"USD"})}</p></div><section className="mt-8"><h2 className="text-2xl font-black">Giving history</h2><div className="mt-4 grid gap-3">{error?<div className="lfp-card p-6 text-red-700">Unable to load your giving history: {error.message}</div>:x?.length?x.map((t:any)=><article key={t.id} className="lfp-card flex items-center justify-between gap-4 p-5"><div><b>{new Date(t.donated_at).toLocaleDateString()}</b><p className="text-sm text-slate-500">{t.source||"L&F General Mission"} · {t.transaction_type}</p>{t.receipt_url&&<a href={t.receipt_url} className="mt-1 inline-flex text-sm font-black text-blue-700">Zeffy receipt →</a>}</div><b>{Number(t.amount).toLocaleString("en-US",{style:"currency",currency:t.currency})}</b></article>):<div className="lfp-card p-6">No synchronized giving records yet.</div>}</div></section><section className="mt-8 rounded-[2rem] border border-blue-100 bg-blue-50/60 p-6"><h2 className="text-xl font-black">Impact, not a dollar-for-outcome claim.</h2><p className="mt-2 leading-7 text-slate-600">As L&amp;F publishes verified ministry updates, this account area can show what the ministry accomplished during the period your generosity helped support collectively.</p></section></div></main>}