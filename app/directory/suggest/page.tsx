import Link from"next/link";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{submitDirectorySuggestion}from"../actions";
export default async function Suggest({searchParams}:{searchParams:Promise<{submitted?:string}>}){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect("/login?next=/directory/suggest");const q=await searchParams;if(q.submitted)return <main className="lfp-page"><div className="lfp-shell py-16"><div className="lfp-card mx-auto max-w-2xl p-8 text-center"><p className="lfp-eyebrow">Received</p><h1 className="mt-3 text-4xl font-black">Thanks for the recommendation.</h1><p className="mt-4 text-slate-600">L&amp;F will review and verify it before anything is published. A suggestion does not create a partnership or public listing automatically.</p><Link href="/directory" className="lfp-button lfp-button-primary mt-6">Return to Directory</Link></div></div></main>;return <main className="lfp-page pb-24"><div className="lfp-shell py-12"><Link href="/directory" className="text-sm font-black text-blue-700">← Directory</Link><p className="lfp-eyebrow mt-8">Suggest a Resource</p><h1 className="mt-3 max-w-3xl text-4xl font-black sm:text-6xl">Know a ministry or resource people should be able to find?</h1><p className="mt-4 max-w-3xl text-lg leading-8 text-slate-600">Recommend it to L&amp;F. Every suggestion is reviewed before publication; this form never publishes directly to the Directory.</p><form action={submitDirectorySuggestion} className="lfp-card mt-8 grid max-w-3xl gap-4 p-7"><input name="submitter_name" placeholder="Your name" className="rounded-xl border p-3"/><input required name="organization_name" placeholder="Organization or resource name" className="rounded-xl border p-3"/><input name="website_url" placeholder="Website (optional)" className="rounded-xl border p-3"/><div className="grid gap-3 sm:grid-cols-2"><input name="city" placeholder="City" className="rounded-xl border p-3"/><input name="state" placeholder="State" className="rounded-xl border p-3"/></div><textarea required name="message" rows={6} placeholder="Why should L&F consider this resource? What does it offer?" className="rounded-xl border p-3"/><button className="lfp-button lfp-button-primary">Send Recommendation</button></form></div></main>}