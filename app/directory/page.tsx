import Link from"next/link";import{createClient}from"@/lib/supabase/server";
const labels:any={lf_ministry:"L&F Ministry",official_partner:"Official L&F Partner",featured_ministry:"Featured Ministry",directory_listing:"Directory Listing",recommended_resource:"Recommended Resource",church:"Church",nonprofit:"Nonprofit",service_organization:"Service Organization",online_resource:"Online Resource"};
export default async function Directory({searchParams}:{searchParams:Promise<{q?:string;type?:string;location?:string}>}){const p=await searchParams,s=await createClient();let q=s.from("directory_entries").select("id,slug,name,entry_type,short_description,services,who_served,location_type,city,state,website_url,verification_status,last_verified_at").eq("status","approved").order("featured",{ascending:false}).order("name");if(p.q)q=q.textSearch("name",p.q,{type:"websearch"});if(p.type)q=q.eq("entry_type",p.type);if(p.location)q=q.eq("location_type",p.location);const{data:x}=await q;return <main className="lfp-page pb-24"><section className="bg-[rgb(var(--lfp-ink))] text-white"><div className="lfp-shell py-16"><p className="text-xs font-black uppercase tracking-[.2em] text-sky-300">Ministry &amp; Resource Directory</p><h1 className="mt-4 max-w-4xl text-4xl font-black sm:text-6xl">Find help. Find community. Find trusted resources.</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-slate-300">A curated directory of L&amp;F ministries and outside organizations or resources we want to help people discover. Relationship labels tell you exactly how each listing relates to L&amp;F.</p></div></section><div className="lfp-shell py-10"><form className="lfp-card grid gap-3 p-5 md:grid-cols-3"><input name="q" defaultValue={p.q} placeholder="Search the directory" className="rounded-xl border p-3"/><select name="type" defaultValue={p.type||""} className="rounded-xl border p-3"><option value="">All types</option>{Object.entries(labels).map(([v,l]:any)=><option key={v} value={v}>{l}</option>)}</select><select name="location" defaultValue={p.location||""} className="rounded-xl border p-3"><option value="">Anywhere</option><option>local</option><option>regional</option><option>national</option><option>online</option><option>global</option></select><button className="lfp-button lfp-button-primary md:col-span-3">Search Directory</button></form><div className="mt-8 grid gap-5 md:grid-cols-2">{x?.length?x.map((a:any)=><article key={a.id} className="lfp-card p-6"><p className="text-xs font-black uppercase tracking-widest text-blue-700">{labels[a.entry_type]||a.entry_type}</p><h2 className="mt-2 text-2xl font-black">{a.name}</h2><p className="mt-3 leading-7 text-slate-600">{a.short_description}</p><p className="mt-4 text-sm font-bold text-slate-500">{[a.city,a.state,a.location_type].filter(Boolean).join(" · ")}</p><div className="mt-5 flex flex-wrap gap-3"><Link href={"/directory/"+a.slug} className="lfp-button lfp-button-secondary">View details</Link>{a.website_url&&<a href={a.website_url} target="_blank" rel="noopener noreferrer" className="lfp-button lfp-button-secondary">Visit website ↗</a>}</div></article>):<div className="lfp-card p-7 md:col-span-2"><h2 className="text-2xl font-black">No matching listings yet.</h2><p className="mt-2 text-slate-600">The Directory is curated and will grow as L&amp;F verifies resources.</p></div>}</div></div></main>}