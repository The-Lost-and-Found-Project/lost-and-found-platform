import Link from"next/link";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";export default async function Page(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect("/login");const{data:p}=await s.from("profiles").select("role").eq("id",user.id).maybeSingle();if(p?.role!=="admin")redirect("/dashboard");const{data:x}=await s.from("giving_campaigns").select("*").order("created_at",{ascending:false});return <main className="lfp-page"><div className="lfp-shell py-10"><Link href="/studio/giving">← Giving & Stewardship</Link><h1 className="mt-6 text-4xl font-black">Campaigns</h1><p className="mt-3 text-slate-600">Focused invitations without automatically creating financial silos. General Mission is the default; Designated must be intentional and clearly disclosed.</p><div className="mt-8 grid gap-4">{x?.length?x.map((c:any)=><article key={c.id} className="lfp-card p-6"><p className="text-xs font-black uppercase text-blue-700">{c.campaign_type.replace("_"," ")} · {c.status}</p><h2 className="mt-2 text-2xl font-black">{c.title}</h2><p className="mt-3 text-slate-600">{c.summary}</p></article>):<div className="lfp-card p-6">No campaigns yet. The campaign system is ready for the first approved L&amp;F campaign.</div>}</div></div></main>}