import Link from"next/link";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";export default async function Page(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect("/login");const{data:p}=await s.from("profiles").select("role").eq("id",user.id).maybeSingle();if(p?.role!=="admin")redirect("/dashboard");const{data:x}=await s.from("ministry_stories").select("*").order("submitted_at",{ascending:false});return <main className="lfp-page"><div className="lfp-shell py-10"><Link href="/studio/giving">← Giving & Stewardship</Link><h1 className="mt-6 text-4xl font-black">Stories &amp; Testimonies</h1><p className="mt-3 text-slate-600">Nothing becomes public automatically. Consent follows the person: private, anonymous public, or named public.</p><div className="mt-8 grid gap-4">{x?.length?x.map((a:any)=><article key={a.id} className="lfp-card p-6"><p className="text-xs font-black uppercase text-blue-700">{a.story_type} · {a.consent} · {a.status}</p><h2 className="mt-2 text-xl font-black">{a.title||"Untitled story"}</h2><p className="mt-3 whitespace-pre-line text-slate-600">{a.body}</p></article>):<div className="lfp-card p-6">No stories have been submitted yet.</div>}</div></div></main>}