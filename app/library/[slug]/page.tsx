import Link from "next/link";
import { notFound, redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";

const sourceLabel=(v:string)=>v==="lfp_original"?"L&F Original":v==="lfp_approved"?"L&F Approved":"Emmaus";
export default async function LibraryItemPage({params}:{params:Promise<{slug:string}>}){
 const {slug}=await params; const supabase=await createClient(); const {data:{user}}=await supabase.auth.getUser(); if(!user)redirect("/login");
 const {data:item}=await supabase.from("content_catalog").select("*").eq("slug",slug).eq("is_published",true).maybeSingle(); if(!item)notFound();
 return <main className="lfp-page pb-24"><section className="relative overflow-hidden"><div aria-hidden className="lfp-grid absolute inset-0"/><div className="lfp-shell relative py-10 sm:py-14"><Link href="/library" className="text-sm font-black text-blue-700">← Library</Link><div className="mt-7 max-w-4xl"><div className="flex flex-wrap items-center gap-2"><span className="rounded-full bg-blue-50 px-3 py-1 text-[10px] font-black uppercase tracking-[.14em] text-blue-700">{sourceLabel(item.provenance)}</span><span className="text-xs font-black uppercase tracking-[.14em] text-slate-400">{item.content_type}</span></div><h1 className="mt-4 text-4xl font-black tracking-[-.04em] text-slate-950 sm:text-6xl">{item.title}</h1>{item.summary&&<p className="mt-5 text-lg leading-8 text-slate-600">{item.summary}</p>}<div className="mt-5 flex flex-wrap gap-3 text-sm font-bold text-slate-500">{item.author_name&&<span>{item.author_name}</span>}{item.duration_minutes&&<span>• {item.duration_minutes} min</span>}{item.difficulty&&<span>• {item.difficulty}</span>}</div></div></div></section><div className="lfp-shell py-8"><div className="grid gap-6 lg:grid-cols-[1fr_.32fr]"><article className="lfp-card p-6 sm:p-8">{item.body?<div className="whitespace-pre-wrap text-[1.05rem] leading-8 text-slate-700">{item.body}</div>:<p className="leading-7 text-slate-600">This resource is linked from another L&F experience.</p>}{item.external_url&&<a href={item.external_url} target="_blank" rel="noopener noreferrer" className="lfp-button lfp-button-primary mt-7">Open resource ↗</a>}</article><aside className="space-y-4"><div className="lfp-card p-5"><p className="lfp-eyebrow">Scripture</p><div className="mt-3 flex flex-wrap gap-2">{(item.scripture_refs??[]).length?(item.scripture_refs??[]).map((x:string)=><span key={x} className="rounded-full bg-slate-100 px-3 py-2 text-sm font-bold">{x}</span>):<span className="text-sm text-slate-500">No references added yet.</span>}</div></div><div className="lfp-card p-5"><p className="lfp-eyebrow">Topics</p><div className="mt-3 flex flex-wrap gap-2">{(item.topics??[]).map((x:string)=><span key={x} className="rounded-full bg-blue-50 px-3 py-2 text-sm font-bold text-blue-700">{x}</span>)}</div></div></aside></div></div></main>
}
