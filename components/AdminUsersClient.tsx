"use client";

import { useMemo, useState } from "react";

const ROLE_OPTIONS=[{value:"member",label:"Member"},{value:"facilitator",label:"Facilitator"},{value:"supervisor",label:"Supervisor"},{value:"admin",label:"Administrator"}];
type UserRow={id:string;full_name:string|null;email:string|null;role:string|null;is_active:boolean;created_at:string};
type Supervision={supervisor_user_id:string;facilitator_user_id:string};
function formatJoinedDate(value:string){const[year,month,day]=value.slice(0,10).split("-");return year&&month&&day?`${Number(month)}/${Number(day)}/${year}`:value;}

export default function AdminUsersClient({users:initialUsers,currentUserId,supervision:initialSupervision}:{users:UserRow[];currentUserId:string;supervision:Supervision[]}){
 const[users,setUsers]=useState(initialUsers);const[supervision,setSupervision]=useState(initialSupervision);const[query,setQuery]=useState("");const[showInactive,setShowInactive]=useState(false);const[busyId,setBusyId]=useState<string|null>(null);const[confirmingId,setConfirmingId]=useState<string|null>(null);const[error,setError]=useState("");
 const supervisors=useMemo(()=>users.filter(u=>u.is_active&&u.role==="supervisor"),[users]);const facilitators=useMemo(()=>users.filter(u=>u.is_active&&u.role==="facilitator"),[users]);
 async function changeRole(userId:string,role:string){setBusyId(userId);setError("");try{const response=await fetch("/api/admin/users/set-role",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({userId,role})});const result=await response.json().catch(()=>({}));if(!response.ok)setError(result.error??"Role could not be updated.");else{setUsers(current=>current.map(member=>member.id===userId?{...member,role}:member));setSupervision(current=>current.filter(x=>x.supervisor_user_id!==userId&&x.facilitator_user_id!==userId));}}catch{setError("Role could not be updated.");}finally{setBusyId(null)}}
 async function setSupervisorLink(supervisorUserId:string,facilitatorUserId:string,assigned:boolean){const key=`${supervisorUserId}:${facilitatorUserId}`;setBusyId(key);setError("");try{const response=await fetch("/api/admin/users/set-supervision",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({supervisorUserId,facilitatorUserId,assigned})});const result=await response.json().catch(()=>({}));if(!response.ok)setError(result.error??"Supervisor assignment could not be updated.");else setSupervision(current=>assigned?[...current.filter(x=>!(x.supervisor_user_id===supervisorUserId&&x.facilitator_user_id===facilitatorUserId)),{supervisor_user_id:supervisorUserId,facilitator_user_id:facilitatorUserId}]:current.filter(x=>!(x.supervisor_user_id===supervisorUserId&&x.facilitator_user_id===facilitatorUserId)));}catch{setError("Supervisor assignment could not be updated.");}finally{setBusyId(null)}}
 async function changeAccess(userId:string,isActive:boolean){setBusyId(userId);setError("");try{const response=await fetch("/api/admin/users/set-active",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({userId,isActive})});const result=await response.json().catch(()=>({}));if(!response.ok)setError(result.error??"Account access could not be updated.");else setUsers(current=>current.map(member=>member.id===userId?{...member,is_active:isActive}:member));}catch{setError("Account access could not be updated.");}finally{setBusyId(null)}}
 async function deleteUser(userId:string){setBusyId(userId);setError("");try{const response=await fetch("/api/admin/users/delete",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({userId})});const result=await response.json().catch(()=>({}));if(!response.ok)setError(result.error??"Account could not be deleted.");else{setUsers(current=>current.filter(member=>member.id!==userId));setSupervision(current=>current.filter(x=>x.supervisor_user_id!==userId&&x.facilitator_user_id!==userId));}}catch{setError("Account could not be deleted.");}finally{setBusyId(null);setConfirmingId(null)}}
 const normalizedQuery=query.trim().toLowerCase();const visibleUsers=users.filter(member=>{if(!showInactive&&!member.is_active)return false;return!normalizedQuery||`${member.full_name??""} ${member.email??""}`.toLowerCase().includes(normalizedQuery)});
 return <main className="lfp-page pb-20"><section className="bg-slate-950 text-white"><div className="lfp-shell py-12 sm:py-16"><p className="text-xs font-black uppercase tracking-[0.2em] text-amber-300">Administration</p><h1 className="mt-4 text-4xl font-black tracking-tight sm:text-6xl">People & Roles</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-indigo-100/75">Member → Facilitator → Supervisor → Administrator. Facilitators only reach their groups. Supervisors only reach facilitators assigned below them. Administrators retain global access.</p><a href="/admin" className="lfp-button mt-7 border border-white/20 bg-white/10 text-white">Back to Administration</a></div></section><div className="lfp-shell py-10 sm:py-14">{supervisors.length>0&&facilitators.length>0&&<section className="lfp-card mb-7 p-5 sm:p-7"><h2 className="text-2xl font-black">Leadership hierarchy</h2><p className="mt-2 text-sm text-slate-600">Assign facilitators to supervisors. A supervisor can then access only the groups and members belonging to those facilitators.</p><div className="mt-5 grid gap-4">{supervisors.map(supervisor=><article key={supervisor.id} className="rounded-2xl border bg-slate-50 p-4"><h3 className="font-black">{supervisor.full_name||supervisor.email}</h3><p className="text-xs text-slate-500">Supervisor</p><div className="mt-3 grid gap-2 md:grid-cols-2">{facilitators.map(f=>{const assigned=supervision.some(x=>x.supervisor_user_id===supervisor.id&&x.facilitator_user_id===f.id);const key=`${supervisor.id}:${f.id}`;return <label key={f.id} className="flex items-center gap-3 rounded-xl bg-white p-3 text-sm"><input type="checkbox" checked={assigned} disabled={busyId===key} onChange={e=>setSupervisorLink(supervisor.id,f.id,e.target.checked)}/><span><strong>{f.full_name||f.email}</strong>{f.email&&<small className="block text-slate-500">{f.email}</small>}</span></label>})}</div></article>)}</div></section>}<div className="lfp-card p-5 sm:p-7"><div className="flex flex-wrap items-end gap-4"><label htmlFor="user-directory-search" className="min-w-64 flex-1 text-sm font-black text-slate-800">Find a member<input id="user-directory-search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Name or email" className="mt-2 block min-h-11 w-full rounded-xl border border-slate-300 px-3 py-2 font-normal"/></label><label className="flex min-h-11 items-center gap-2 text-sm text-slate-700"><input type="checkbox" checked={showInactive} onChange={e=>setShowInactive(e.target.checked)}/> Show inactive accounts</label></div>{error&&<p className="mt-4 rounded-xl border border-rose-200 bg-rose-50 p-3 text-rose-800">{error}</p>}<p className="mt-4 text-sm text-slate-500">Showing {visibleUsers.length} account{visibleUsers.length===1?"":"s"}.</p><div className="mt-5 space-y-3">{visibleUsers.map(member=>{const busy=busyId===member.id;const isSelf=member.id===currentUserId;const role=ROLE_OPTIONS.some(o=>o.value===member.role)?String(member.role):"member";const accessibleName=member.full_name??member.email??"unnamed member";return <article key={member.id} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="grid gap-5 lg:grid-cols-[1fr_auto] lg:items-center"><div><div className="flex flex-wrap items-center gap-2"><h2 className="font-black text-slate-950">{member.full_name||"Unnamed member"}</h2><span className={`rounded-full px-2 py-1 text-xs font-bold ${member.is_active?"bg-emerald-50 text-emerald-800":"bg-rose-50 text-rose-800"}`}>{member.is_active?"Active":"Inactive"}</span>{isSelf&&<span className="rounded-full bg-indigo-50 px-2 py-1 text-xs font-bold text-indigo-800">You</span>}</div><p className="mt-1 text-sm text-slate-600">{member.email}</p><p className="mt-1 text-xs text-slate-400">Joined {formatJoinedDate(member.created_at)}</p></div><div className="flex flex-wrap items-end gap-3"><label className="text-xs font-bold uppercase tracking-wide text-slate-500">Role<select aria-label={`Role for ${accessibleName}`} value={role} disabled={busy||isSelf} onChange={e=>changeRole(member.id,e.target.value)} className="mt-1 block min-h-11 rounded-xl border border-slate-300 px-3 py-2 text-sm font-normal normal-case tracking-normal text-slate-800 disabled:opacity-60">{ROLE_OPTIONS.map(option=><option key={option.value} value={option.value}>{option.label}</option>)}</select></label><button type="button" disabled={busy||isSelf} onClick={()=>changeAccess(member.id,!member.is_active)} className="lfp-button border border-slate-300 bg-white text-slate-800 disabled:opacity-50">{member.is_active?"Deactivate":"Reactivate"}</button>{!isSelf&&(confirmingId===member.id?<span className="flex items-center gap-2"><button type="button" disabled={busy} onClick={()=>deleteUser(member.id)} className="lfp-button bg-rose-600 text-white">Confirm Delete</button><button type="button" onClick={()=>setConfirmingId(null)} className="lfp-button border border-slate-300 bg-white text-slate-800">Cancel</button></span>:<button type="button" disabled={busy} onClick={()=>setConfirmingId(member.id)} className="lfp-button border border-rose-200 bg-rose-50 text-rose-800">Delete</button>)}</div></div></article>})}</div></div></div></main>;
}
