"use client";
import {useState} from "react";
export default function ZeffySyncButton(){const[state,setState]=useState<"idle"|"running"|"done"|"error">("idle"),[message,setMessage]=useState("");async function sync(){setState("running");setMessage("");try{const r=await fetch("/api/admin/giving/zeffy-sync",{method:"POST"});const b=await r.json();if(!r.ok)throw new Error(b?.error||"Sync failed");setMessage(`Synced ${b.upserted} of ${b.seen} Zeffy payments.`);setState("done")}catch(e:any){setMessage(e?.message||"Sync failed");setState("error")}}return <div><button onClick={sync} disabled={state==="running"} className="lfp-button lfp-button-primary">{state==="running"?"Syncing Zeffy...":"Sync Zeffy Now"}</button>{message&&<p className={`mt-2 text-sm ${state==="error"?"text-red-700":"text-emerald-700"}`}>{message}</p>}</div>}