type Section={title?:string;scripture?:string;teaching?:string;story?:string;discussion?:string[];journal?:string[];prayer?:string};

export default function GuidedContent({sections}:{sections:Section[]}){
 if(!Array.isArray(sections)||!sections.length)return null;
 return <div className="space-y-5">{sections.map((s,i)=><section key={`${s.title}-${i}`} className="rounded-[1.6rem] border border-slate-200 bg-white p-5 sm:p-7"><p className="text-[11px] font-black uppercase tracking-[.16em] text-blue-700">Part {i+1}</p>{s.title&&<h2 className="mt-2 text-2xl font-black text-slate-950">{s.title}</h2>}{s.scripture&&<div className="mt-4 rounded-2xl bg-blue-50 p-4 font-black text-blue-900">{s.scripture}</div>}{s.teaching&&<div className="mt-4 whitespace-pre-wrap leading-8 text-slate-700">{s.teaching}</div>}{s.story&&<div className="mt-5 rounded-2xl bg-amber-50 p-4"><p className="text-xs font-black uppercase tracking-[.14em] text-amber-800">Relatable story</p><p className="mt-2 leading-7 text-amber-950">{s.story}</p></div>}{s.discussion?.length?<div className="mt-5"><h3 className="font-black text-slate-950">Discussion</h3><ol className="mt-2 space-y-2 text-slate-700">{s.discussion.map((q,j)=><li key={q}><span className="font-black text-blue-700">{j+1}.</span> {q}</li>)}</ol></div>:null}{s.journal?.length?<div className="mt-5"><h3 className="font-black text-slate-950">Journal</h3><ul className="mt-2 space-y-2 text-slate-700">{s.journal.map(q=><li key={q}>• {q}</li>)}</ul></div>:null}{s.prayer&&<div className="mt-5 border-t border-slate-200 pt-5"><p className="text-xs font-black uppercase tracking-[.14em] text-violet-700">Prayer</p><p className="mt-2 italic leading-7 text-slate-700">{s.prayer}</p></div>}</section>)}</div>
}
