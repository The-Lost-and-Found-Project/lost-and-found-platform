export type TraversalNode={id:string;title:string;nodeType:string;scriptureReference?:string|null};
export type TraversalEdge={from:string;to:string;relationshipKey:string;confidence:number;weight?:number};
export const RELATIONSHIP_PRIORITY=['fulfilled_prophecy','type_antitype','covenant','parallel_account','quotation','theme','greek_word','hebrew_word','person','place'];
export function nextDiscoveries(currentId:string,edges:TraversalEdge[],nodes:TraversalNode[],limit=5){const byId=new Map(nodes.map(n=>[n.id,n]));return edges.filter(e=>e.from===currentId||e.to===currentId).sort((a,b)=>score(b)-score(a)).slice(0,limit).map(e=>{const next=e.from===currentId?e.to:e.from;return{node:byId.get(next),relationship:e.relationshipKey,confidence:e.confidence,score:score(e)}}).filter(x=>x.node);} 
function score(e:TraversalEdge){const priority=Math.max(0,RELATIONSHIP_PRIORITY.length-RELATIONSHIP_PRIORITY.indexOf(e.relationshipKey));return priority*100+e.confidence+(e.weight??0);} 
export function buildDiscoveryQuestion(title:string,relationship:string,target:string){switch(relationship){case 'fulfilled_prophecy':return `Before reading further, what evidence suggests that ${title} fulfills or anticipates ${target}?`;case 'type_antitype':return `What patterns do you notice between ${title} and ${target}?`;case 'parallel_account':return `How does comparing ${title} with ${target} deepen your understanding?`;default:return `Emmaus found a ${relationship.replaceAll('_',' ')} connection to ${target}. What relationship do you observe?`;}}